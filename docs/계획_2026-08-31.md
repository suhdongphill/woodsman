---
날짜: 2026-08-31 (계획)
작성: Claude Opus 5 (Claude Code) · 2026-08-30 작업 종료 시점 기록
관련: `web/CHANGELOG.md`의 2026-08-30 (1)~(20) · `docs/설계_*.md` 넷
---

# 다음에 할 일

> 오늘 사이트가 **계좌 중심 다크**에서 **흐름 중심 크림 매거진**으로 바뀌었다.
> 배포 15회, 테스트 843 → 941개. 자세한 것은 `web/CHANGELOG.md` 2026-08-30 항목 스무 개.

---

## 0. ⚠ 바로 처리할 것 (10초)

- **`/admin/calendar`의 2026-12-09 FOMC 중복 삭제** — 같은 항목이 두 개다.
  타임아웃 난 저장이 실제로는 성공했는데 다시 넣었다. 휴지통 아이콘 한 번이면 된다.

---

## 1. 오늘 세워진 규칙 (⚠ 앞으로 지킨다)

| 규칙 | 어디에 |
|---|---|
| **변경은 한 번에 하나씩 배포** | 운영지침 §3-1 |
| **지표를 움직일 만한 변경만** 릴리스 기록 (파비콘·오타는 안 함) | 운영지침 §3-1 |
| **배포 후 독자의 눈으로 훑는다** | 운영지침 §3-2 |
| **`npm run check`의 종료코드를 직접 읽는다** (`\| tail`은 tail의 코드다) | CLAUDE.md §7 |
| **값이 아니라 정책을 고정한다** (테스트) | `design-policy.test.ts` |
| **모르는 것을 아는 것처럼 적지 않는다** | 오늘만 세 번 걸렸다 — 시각·자금유입·주어 없는 등급 |

---

## 2. 콘텐츠 (⭐ 지금 가장 값이 크다)

사이트의 뼈대는 섰다. **채울 것이 없는 것이 유일한 병목**이다.

- **글을 올린다.** 제목만 치면 주소가 자동으로 붙는다. 편집 버튼도 이제 제대로 먹는다.
- **캘린더가 다음 글을 알려 준다.** 9/11 CPI가 지나가면 `/admin/calendar`의
  「평가가 밀린 일정」에 뜬다. 글을 쓰고 주소를 이으면 그 글이 다음 CPI 때 다시 읽힌다.
- ⚠ **30년-2년 금리차가 지금 0.99%p다.** 2025년 초 0.40 → 연말 1.40 → 지금 0.99.
  구축효과·곡선 기울기는 **글 한 편이 이미 준비된 소재**다.

---

## 3. 사람만 할 수 있는 것

| # | 할 일 | 왜 |
|---|---|---|
| ⭐ | `/admin/settings` **환율 1,350 → 1,409.9** | 틀린 평가액이 쌓이기 전에 |
| ⭐ | `/admin/model-portfolio` **목표 구성비 + 보유 종목** | 홈 계좌 띠와 `/portfolio` 막대가 산다 |
| | `/admin/journal` **스냅숏 1건** | 한 점이면 곡선이 되살아나고, 히어로 숫자가 「추적 중인 지표」에서 원래 셋으로 돌아온다 |
| | `/admin/stocks` **첫 보고서 발행** | `/stocks`·홈이 동시에 산다 |
| | **애드센스 ID 4개** | `/admin/ads`에 넣고 스위치만 켜면 된다. ⚠ 켜기 전 `/privacy` 문장을 현재형으로 |
| | `/bubble-review` **버블 채점 갱신** | 가장 오래된 판정이 2026-03-28 |

---

## 4. 코드 쪽

### 이어서 할 것

- **편집 프레임 2단계** — 데스크톱 **사이드 레일**(콘텐츠 + 우측 거시 요약 고정). 1.0 세션
  (1단계 가로 스트립은 오늘 배포했다)
- **홈 재편 Step 4~6** — `/portfolio` 보강 · SEO 점검 · 문서 (`docs/설계_홈_콘텐츠중심_재편.md`)
- **UI 개편 U4** — 관리자 16화면 두 모드 눈 검수 (공개 화면은 끝났다)
- **측정 루프 M4** — `/admin/analytics` 곡선 위에 릴리스 세로선 겹쳐 보기

### 준비만 해 둔 것

- **다국어** — ⚠ 지금 하면 손해다(설계서 §5-1). 새 화면의 문자열은 파일 위쪽 `TEXT` 상수로
  모으는 관례를 이미 지키고 있다. 나중에 3~5 세션.
- **캘린더 자동 수집** — `source`·`externalId` 자리를 만들어 뒀다. FRED 발표일정부터가 맞다.
  ⚠ 크롤링은 하지 않는다.
- **진짜 자금유입(AUM·설정주식수)** — 유료 데이터가 붙으면 그때 **정확한 이름**을 달 수 있다.
  지금은 「가격이 만든 결과」로만 적는다.

### 남은 점검 항목

- 중간 1건 — `lib/beacon-guard.ts` 비콘 상한이 병렬에 안 듣는다.
  후속안은 **엣지 WAF Rate Limiting**. ⚠ 무료 등급에서 막히면 우회 설계하지 말고 유료 전환을 먼저 알린다.
- 커뮤니티를 열기 전 — 게스트 댓글 속도 제한이 (글, 비로그인) 한 통을 공유한다 · `/privacy` 갱신

---

## 5. 기록에 대하여

- `docs/개발사_Woodsman.html`에 **오늘 이야기를 아직 안 옮겼다.** 책 원고가 목적이므로
  ⚠ 결과가 아니라 **무엇과 무엇이 부딪혔는지**를 남긴다. 오늘의 후보:
  - 「편집 버튼이 죽어 있었다」 — 화면은 다 그려졌는데 클릭이 삼켜지던 이야기
  - 「테스트가 옛 디자인을 붙잡고 있었다」 — 좋은 장치가 개편을 막았을 때
  - 「순서를 지킨다던 테스트가 자기 자신을 검사하고 있었다」
  - 「자금 유입이라고 부르지 않기로 했다」 — 없는 데이터에 이름을 붙이지 않는 일
- 설계서 넷은 **버전 이력**을 갖고 있다. 고칠 때 이력에 한 줄씩 더한다.
