---
날짜: 2026-08-06
작성: Claude Opus 5 (Claude Code)
유형: 일일 작업 기록
관련: web/CHANGELOG.md (모듈별 "왜"), docs/woodsman_개발요구서_v2.md (기준 문서)
---

# 2026-08-06 작업 기록

> 이 문서는 **하루 단위 요약**이다. 모듈별로 "왜 그렇게 했나"는 `web/CHANGELOG.md`에 있고,
> 여기서는 무엇을 했고 무엇이 남았는지를 한눈에 본다.

---

## 0. 한 줄 요약

**목업이던 화면 다섯 곳을 전부 DB로 옮기고**, 볼트에만 있던 거시 지표·버블 모니터를
사이트로 들여왔다. 글을 쓸 수 있게 됐고(3모드 편집기), AI가 참고할 기록 창고와
조회수 집계가 붙었다. 운영 배포까지 완료.

---

## 1. 오늘 끝낸 것

| Phase | 내용 | 결과 |
|---|---|---|
| P4 | 대표 포트폴리오(ModelHolding·Rebalance) DB화 + CRUD | ✅ |
| P5 | 콘텐츠(Post) DB화 + 3모드 편집기 + 섹션 프레임 | ✅ |
| P9 | 거시 지표 대시보드 (9묶음 44지표) + FRED/Yahoo 자동 수집 | ✅ |
| P10 | AI·반도체 버블 모니터 (5레이어 28지표) | ✅ |
| P11 | 사이트 로컬 RAG 구조체 (AI 인사이트용) | ✅ |
| P12 | 조회수 집계 · 사용자 삭제 | ✅ |
| — | `/simplify` 정리 리뷰 4종 반영 (재사용·단순화·효율·계층) | ✅ |

### 반복해서 나온 같은 사고

화면이 `lib/mock.ts`를 읽고 있어 **버튼이 아무 데도 연결돼 있지 않던 곳**이
오늘 하루에만 다섯 곳이었다: 대표 포트폴리오 · 콘텐츠 · 홈 편집 · 사용자 · 관리자 대시보드.

전부 같은 처방으로 고쳤다 — `features/<도메인>/repository.ts`로 D1을 읽고,
목업 export를 **삭제**한 뒤, **테스트로 부활을 막았다**(`mock.test.ts`).

---

## 2. 새로 생긴 것

### 순수 모듈 (전부 테스트 있음)

`macro/{registry,series,signal,parse}` · `bubble/{catalog,score}` · `ai/retrieval`
`markdown` · `sanitize-html` · `seo` · `sections` · `manual-price` · `analytics` · `user-delete`

### 데이터 모델

`MacroPoint` · `MacroIngest` · `BubbleReading` · `BubbleTriggerState` · `PageView`
`Post`에 `body`/`format`/`section` 추가, `ModelHolding`에 `price`/`priceAsOf` 추가.
마이그레이션 5건 — 운영 D1 적용 완료.

### 화면

- 공개: `/macro` 허브 · `/macro/[group]` 9장 · `/macro/bubble` · 섹션 프레임(홈·거시·포트폴리오)
- 관리자: `/admin/macro` · `/admin/bubble` · `/admin/posts`(편집기) · `/admin/users`(삭제)
- `/admin/ai`에 지식 검색(RAG) 패널

---

## 3. 오늘 실제로 터진 것과 처방

| 증상 | 원인 | 처방 |
|---|---|---|
| 자료 가져오기 중 D1 연결 끊김(ECONNRESET) | 지표 4개를 병렬로 처리하며 시계열 수백 점을 동시에 write | 네트워크는 병렬, **DB 쓰기는 순차**. 한 점당 한 문장 대신 **20행을 한 INSERT**로 |
| 한 번 수집에 3분 34초 | 매번 전 구간을 다시 씀 | 마지막 기준일에서 **60일만 되감기** → **28초** |
| 액션 호출이 500 | `"use server"` 파일에서 상수를 export | `form-state.ts`로 분리 (**두 번 겪음**) |
| 배포 실패(8/2부터 누적) | GitHub Actions에 Cloudflare 시크릿 미등록 | 로컬 wrangler로 배포. **시크릿 등록은 사용자 몫**(아래 4장) |
| 로컬 dev 500 | `npm run build`를 dev 위에서 실행해 `.next` 손상 | dev를 끄고 빌드(CLAUDE.md 7장의 그 규칙) |

---

## 4. ⚠ 남은 일 — 사람이 해야 하는 것

1. **GitHub Actions 시크릿 등록** (자동 배포가 8/2부터 실패 중)
   ```
   gh secret set CLOUDFLARE_API_TOKEN
   gh secret set CLOUDFLARE_ACCOUNT_ID --body e24cd58c5b44dbdc6b61ab85b2be0401
   ```
   그때까지 배포 경로: `npm run d1:migrate && npm run cf:deploy`

2. **거시 지표 첫 수집** — `/admin/macro` → "전체 자료 가져오기" 한 번(약 30초).
   누르기 전까지 `/macro`는 "미수집"으로 정직하게 표시된다.

3. **버블 모니터 근거 채우기** — 점수 28개는 시드로 들어갔지만 **근거 문장은 비어 있다.**
   `/admin/bubble`에서 적는다(그게 이 모니터의 값이다).

---

## 5. 정리 리뷰에서 잡힌 것 (`/simplify`)

리뷰 4종을 병렬로 돌려 나온 지적을 반영했다. 가장 값진 것 하나:

> ⚠ `normalizePath`가 `/\evil.com`을 통과시켰다. 그 경로는 관리자 화면에 **링크로 렌더**된다.
> 이미 테스트가 있는 `access.safeNextPath`를 쓰지 않고 직접 만든 것이 원인 —
> **같은 판단을 두 번 구현하면 한쪽이 반드시 뒤처진다.**

나머지: 개수를 세려고 목록을 통째로 읽던 것 → 개수 질의, 비콘의 두 write → 한 batch,
기간 판단 → `lib/analytics.ts`로 올려 테스트 부착, 대시보드 인라인 차트 → 컴포넌트 분리.

남겨 둔 것: 티스토리 클릭 집계와 조회 집계의 **공통 일별 카운터 추출**
(동작하는 코드를 배포 직전에 건드리지 않았다).

---

## 6. 다음 작업 후보

- P5.5 티스토리 RSS 수집 → Post 자동 등록
- P6 AI 실제 호출 연결 (지금은 라우팅·프롬프트·RAG까지)
- P7 AdSense (콘텐츠 축적 후)
- **AI 친화 사이트(GEO)** — 설계 계획서 별도 작성: `docs/계획_AI친화_사이트_GEO.md`

---

## 7. 확인한 것

- `npm run check` 통과 — 테스트 **319개**
- 운영 배포 2회(로컬 wrangler 경로). 마이그레이션 5건 반영 완료
- 운영(`portfolio-solutions.net`) 전 경로 200, 버블 모니터 **54.3점 · 경계** 표시 확인
- 조회 비콘(운영): 브라우저 UA는 집계, `GPTBot`·`Googlebot`은 제외,
  `/\evil.com` 같은 경로는 기록 거부되는 것까지 확인
- ⚠ 관리자 화면 일부(사용자 삭제)는 브라우저 확장이 끊겨 **육안 확인을 못 했다.**
  로직은 테스트로 고정(자기 자신·마지막 관리자 삭제 차단).
