---
날짜: 2026-08-26 (계획)
작성: Claude Opus 5 (Claude Code) · 2026-08-25 작업 종료 시점 기록
관련: `docs/계획_2026-08-23.md` · `web/CHANGELOG.md`의 2026-08-25
---

# 다음에 할 일

> 오늘은 **계획 6·7번(코드)만** 했다. §1은 관리자 로그인이 필요해 손대지 않았다.
> ⚠ **네 번째 세션째 §1이 그대로다. 병목은 코드가 아니다.**

---

## 0. 오늘 한 일

| 무엇 | 결과 |
|---|---|
| `lib/macro/derived.ts` — 파생 계열 + **순유동성(netliq)** | ✅ 테스트 17개 |
| 로컬 D1에 FRED 실데이터를 넣고 **화면에서 확인** (5.81조) | ✅ |
| 볼트와 신선도 기한 통일 (JOLTS 105 · 케이스-실러 135) | ✅ 3곳 |
| 볼트 유동성 인사이트를 지표 설명에 반영 | ✅ |

테스트 816 → **833개.** 자세한 것은 `web/CHANGELOG.md` 2026-08-25.

### ⚠ 아직 배포하지 않았다

`npm run check`는 통과했고 로컬 화면도 봤지만 **운영 배포는 안 했다.**
배포 전에 아래 1-7을 먼저 누르는 편이 낫다 — 순유동성은 성분 값이 없으면 화면에서
「아직 수집 안 됨」으로만 보여서, 새로 만든 것이 제대로 도는지 운영에서 확인할 수 없다.

### ⚠ 사이트 값을 하나 고쳤다 (계획과 다르게)

계획 7번은 "볼트에 **사이트 값을** 되돌린다"였는데, 다시 재 보니 사이트의 JOLTS 100이
**정상 상태의 최대 나이와 같은 값**이라 여유가 0이었다. 발표 며칠 전마다 회색이 된다.
볼트가 같은 계산으로 이미 105를 쓰고 있어 **사이트를 105로 올렸다.**

### ⚠ 볼트와 일부러 다르게 둔 것 — 사람이 정할 일

**미시간 소비심리.** 볼트는 예외 105로 회색을 끄고, 사이트는 예외 없이 기한초과로 띄운다.

| | 판단 |
|---|---|
| 볼트 | "FRED 반영이 2개월 늦는 건 우리가 아는 정상이다" |
| 사이트 | "읽는 사람이 그걸 알 리 없으니 낡았다고 말해야 한다" |

둘 다 일리가 있어 **어느 쪽으로 통일할지 정해 주면** 그대로 맞춘다.

---

## 1. ⚠ 사람이 해야 하는 것 (네 번째 세션째)

| # | 할 일 | 상태 |
|---|---|---|
| **1-7** | `/admin/macro` **전체 자료 가져오기** | ⭐ **오늘도 1순위.** 유동성 4 + 금리 4 + DXY·금이 여전히 값 0건이고, **오늘 만든 순유동성도 여기 걸려 있다** |
| 1-6 | `/admin/사이트 기본값`의 **환율** | ⚠ `1,350원`인데 거시 지표는 `1,409.9원`. 4.4% 어긋난 채로 모든 평가액이 계산된다 |
| **1-1** | `/admin/model-portfolio` **목표 구성비** | ⭐ 정하는 순간 홈·`/portfolio` 막대가 산다 |
| **1-3** | 같은 화면 **실제 보유 종목** | 티커 조회 작동 확인됨(8/21 NVDA) |
| **1-2** | `/admin/journal` **계좌 스냅숏 1건** | 한 점만 있어도 `/portfolio` 곡선이 되살아난다 |
| **1-4** | `/admin/stocks` **첫 보고서 발행** | 발행해야 `/stocks`·홈·종목 카드가 동시에 산다 |
| 1-5 | `/admin/stocks` 「전체 시세 가져오기」 | ⚠ 운영에서 여전히 0회. 1-4 다음이 맞다 |
| 1-8 | `/admin/bubble` `llm_token_spend`·`asset_life_mismatch` | 28/30 → 30/30 |
| 1-9 | ⚠ **버블 채점 갱신** (`/bubble-review`) | ⭐ 가장 오래된 판정이 **2026-03-28**. 다섯 달 묵었다 |
| 1-10 | `gh secret set CLOUDFLARE_API_TOKEN` · `CLOUDFLARE_ACCOUNT_ID` | ⚠ **8/2부터 미완.** 배포를 계속 손으로 한다 |

---

## 2. 코드 쪽에 남은 것

### 볼트에서 아직 안 옮긴 것

- **판정 원장(`_calls/`)** — 반증조건·확인시점·자동 스냅숏. "이 사이트가 그때 뭐라고 했고 맞았나"를
  공개할 수 있다. ⚠ 다만 **표본이 쌓여야 값이 난다.**
- **자본흐름 지도** — 볼트가 8/25에 크게 손봤다(`_apps/자본흐름 지도.html` + `_scripts/relations.json`).
  오늘은 **결론만 지표 설명에 옮겼고 그림·근거등급 구조는 안 옮겼다.**
  ⚠ 이건 관계 그래프·D1 파이프라인이 얽힌 큰 작업이라 `/macro-pipeline` 쪽에서 다룰 일이다.
- **EIA 원유재고 3종** — 소스가 FRED가 아니라 EIA라 어댑터가 하나 더 필요하다.
- **M1 증가율 · ISM 신규주문 · 주가지수(SPX·나스닥·코스피)** — 볼트에는 있고 사이트에는 없다.

### 점검에서 살아 있는 것

- 중간 1건 — `lib/beacon-guard.ts` 비콘 볼륨 상한이 병렬에 안 듣는다.
  ⚠ 후속안은 **엣지 WAF Rate Limiting**(Cloudflare 대시보드). 무료 등급에서 막히면
  **우회 설계하지 말고 유료 전환**을 먼저 알린다.
- 낮음 2건 — `app/admin/model-portfolio` 리밸런싱 이력 수정 진입로 없음 · react 19.1.0 RSC DoS 범위(⚠ 추측)

### 보고서 5단계

C(`StockPeer`·`StockTrigger` 구조화)·D(AI 초안 페르소나) 둘 다 **발행본 0건인 지금은 이르다.**

---

## ⚠ 매번 지킬 것

- `npm run dev`를 **끄고** `npm run check` — **build 뒤에 dev를 띄울 때도** `.next`를 지운다
- 화면을 바꿨으면 **실제로 띄워서 본다.** 값이 없으면 로컬 D1에 실데이터를 넣고서라도 본다(오늘 그렇게 했다)
- **모듈로 쪼갠다.** 판단은 `lib/*`(순수 함수 + 테스트), 질의는 `*-repo`, 액션은 `*-actions`, 화면은 `ui/*`
- **빈 칸을 0으로 만들지 않는다.** 없는 값을 채우면 없는 사실이 생긴다
- **화면이 하지 않는 일을 한다고 적지 않는다**
- **운영 데이터를 두드리는 실측은 먼저 묻는다**
- `web/CHANGELOG.md`에 **"왜"** 를 남긴다
- 배포 후 **운영본을 검증한다.** 로컬 통과가 배포 성공이 아니다
- 지시가 `docs/운영지침.md`와 어긋나면 **먼저 말하고**, 진행 여부는 사람이 정한다
