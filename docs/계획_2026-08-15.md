---
날짜: 2026-08-15
작성: Claude Opus 5 (Claude Code)
관련: `docs/계획_2026-08-12.md`(A3 원본) · `docs/종목분석_보고서_설계서_v1.md` · `web/CHANGELOG.md`
---

# 2026-08-15 — A3(속도 제한) 처리와 다음 순서

> 8/12 계획의 **A3이 최우선**이었고 오늘 그것부터 했다.
> 결론: **원인 가설이 틀렸고, 방어 구조 자체를 바꿨다.** 다만 **볼륨 상한은 아직 미해결**이다.

---

## 1. 오늘 끝낸 것 (A3)

| # | 한 일 | 산출물 |
|---|---|---|
| 1 | 8/11 1순위 가설(OpenNext env 프록시) **반증** — 운영에는 프록시가 없다 | 아래 §2 |
| 2 | 방어를 3층으로 분리, 1층(경로 검증)을 새로 만듦 | `src/lib/beacon-path.ts` + 테스트 |
| 3 | 바인딩 모양·반환값 검증, 빈 `catch {}` 제거 | `src/lib/beacon-guard.ts` |
| 4 | **"실제로 막히는지" 재는 화면** | `/admin/diagnostics` · `src/lib/beacon-selftest.ts` |
| 5 | 설정↔코드 상수 대조 테스트 | `beacon-selftest.test.ts` |
| 6 | 원칙 문서 갱신 | `docs/운영지침.md` §5·§6 · `web/CLAUDE.md` §2-1·§3 |

`npm run check` 통과 · 테스트 **420개**(398 → +22).

---

## 2. ⚠ 8/12 계획의 A3 가설은 **틀렸다** (기록으로 남긴다)

8/12 계획서가 적은 1순위 가설:

> "OpenNext env 프록시가 RateLimit 객체를 제대로 넘기지 못한다 — 가장 유력하다."

**어댑터 소스로 반증했다.**

- `@opennextjs/cloudflare/dist/cli/templates/init.js` → `runWithCloudflareRequestContext()`가
  워커의 **실제 `env`를 그대로** AsyncLocalStorage에 넣는다. 운영에는 프록시가 없다.
  프록시(`getPlatformProxy()`)는 **`next dev`에서만** 낀다.
- `lib/d1.ts`도 **같은 `getCloudflareContext()`** 로 바인딩을 꺼내 잘 동작한다.

따라서:
- ⚠ **대안 "미들웨어(`src/middleware.ts`)로 옮긴다"는 폐기한다.** 같은 `env`를 본다.
  (미들웨어는 이미 있다 — 인증 보호용이고 이 문제와 무관하다.)
- ⚠ 남은 설명은 Cloudflare 문서가 스스로 밝힌 성질이다 — *permissive · eventually
  consistent · **정확한 계수 시스템으로 쓰지 말 것***. 8/12 계획의 가설 2가 맞았다.

**교훈:** 남이 만든 불투명한 장치 하나에 방어를 통째로 걸었던 것이 구조적 원인이다.

---

## 3. ⚠ 아직 안 막는 것 — 볼륨 상한 (A3의 남은 절반)

지금 상태를 정직하게 적는다.

| 공격 | 지금 |
|---|---|
| 없는 화면(`/__probe`)을 통계표에 만들기 | **막힌다** (오늘 닫음) |
| 남의 사이트에서 비콘 부르기 | 보조적으로 걸린다(위조 가능한 헤더라 보호로 치지 않는다) |
| **실재 화면의 조회수·체류시간 부풀리기** | ⚠ **못 막는다** |

그래서 `/admin/analytics`의 초기 숫자는 그만큼 덜 믿는다.

### 다음에 고를 수 있는 길 (아직 결정 안 함 — 사람이 정한다)

| 안 | 장점 | 단점 |
|---|---|---|
| **A. WAF Rate Limiting 규칙** | 앱 코드 밖이라 확실. 무료 등급도 규칙 1개 제공 | 대시보드/API 작업이 필요(사람). 규칙 1개를 여기 쓰면 다른 데 못 쓴다 |
| **B. Durable Object 카운터** | 결정적이고 우리가 테스트할 수 있다. Cloudflare가 문서로 권하는 방식 | ⚠ **OpenNext가 워커 엔트리를 생성**하므로 커스텀 DO를 export하려면 `main` 래퍼가 필요하다 — 어댑터 업그레이드마다 깨질 위험 |
| **C. 서명 토큰(HMAC)** | 인프라 없이 우리 코드로 끝난다. 페이지를 렌더해야 토큰이 나온다 | 같은 창 안에서 **재생(replay)** 가능 → 볼륨 상한이 못 된다. 정적 캐시된 페이지에서 토큰이 굳는 문제도 있다 |
| **D. D1 카운터** | — | ⚠ **하지 않는다.** 요청마다 쓰기가 늘어 막으려던 문제를 키운다 |

**권장은 A.** 볼륨 상한은 성격상 앱 밖에서 거는 것이 맞고, B는 어댑터에 묶이는 위험이
이득보다 크다. 다만 A는 **사람이 눌러야** 하므로 아래 §6에 적었다.

⚠ **먼저 할 일은 결정이 아니라 측정이다.** 배포한 뒤 `/admin/diagnostics`의
"속도 제한 재 보기"를 눌러 본다. 진단 바인딩(10회/10초)에서도 안 막히면 이 API를
쓰는 길은 완전히 닫히고, 막히면 8/11의 실패는 **60회/60초라는 설정 쪽 문제**가 된다.
그 결과에 따라 A/B가 갈린다.

---

## 4. 다음 작업 — 종목 보고서 1단계 (8/12 계획 B, 그대로 유효)

A3이 정리됐으니 다음은 B다. **화면 없이도 검증되는 순수 모듈부터**.

| # | 작업 | 산출물 |
|---|---|---|
| B1 | `lib/canslim/score.ts` — 가중 100점·composite10·등급 밴드(**8.0/6.5**)·M 게이트(M≤3 보류)·⚠ **N/A는 분모에서 제외** | `canslim.test.ts` |
| B2 | `lib/report/catalog.ts` — 10섹션 정의(필수/선택) + 섹션별 필수 필드 | — |
| B3 | `lib/report/rules.ts` — 정직성 규율 7가지 검증기. R2·R3·R5·R6 미충족이면 **발행 차단** | `report.test.ts` |
| B4 | ⚠ `/stocks/[ticker]`의 `CANSLIM_SCORES` 하드코딩과 `?? 0`을 **이때 지운다** | `mock.test.ts` 차단 추가 |

⚠ B4를 미루지 않는다. 새 모듈이 생겨도 화면이 옛 하드코딩을 읽으면 아무것도 안 바뀐다
(그 병을 이미 여섯 곳에서 겪었다). 현재 위치는 `src/app/(public)/stocks/[ticker]/page.tsx:23`,
`src/features/ai/ui/CanslimPanel.tsx:53`이고 **등급 경계가 7.0/5.0로 정책(8.0/6.5)보다 후하다.**

---

## 5. 그 뒤 (변동 없음)

| 순서 | 작업 | 근거 |
|---|---|---|
| C1 | 보고서 2단계 — 스키마 + repository + 관리자 편집 화면 | 설계서 §7 |
| C2 | 종목 시세 실데이터 | `계획_2026-08-07_미세작업.md` C1~C7 — **마지막 남은 목업** |
| C3 | 볼트↔사이트 카탈로그 대조를 `site-inspect` 스킬에 추가 | 아이디어 노트 B1 |
| C4 | RSS 수집(P5.5) · GEO 2~4단계 | 8/7 계획 D·E |

- **화면 통계 첫 판독** — 표본이 며칠 쌓였다. 다만 §3이 열려 있는 동안의 숫자다.
- 8/7 점검의 **미검증 19건** — 보안 헤더 부재, 로그인 시도 제한 없음 등.

---

## 6. ⚠ 사람이 해야 하는 것 (자동화 불가)

8/2부터 밀리고 있는 셋은 그대로다.

| # | 할 일 | 상태 |
|---|---|---|
| 0-1 | `gh secret set CLOUDFLARE_API_TOKEN` · `CLOUDFLARE_ACCOUNT_ID` | ⚠ 8/2부터 미완. 자동 배포 실패 중 |
| 0-2 | `/admin/macro` → "전체 자료 가져오기" (운영 첫 수집) | ⚠ 아직 안 누름 |
| 0-3 | `/admin/bubble`에서 `llm_token_spend`·`asset_life_mismatch` 채점(각 2점) | 넣어야 볼트(56 · 30/30)와 맞는다 |
| **0-4** | **오늘 것 배포 후 `/admin/diagnostics` 버튼 누르기** | §3의 갈림길이 이 결과로 정해진다 |
| 0-5 | (§3에서 A안을 고르면) Cloudflare WAF Rate Limiting 규칙 1개를 `/api/view`·`/api/engagement`에 건다 | 결정 후 |

---

## ⚠ 매번 지킬 것

- `npm run dev`를 **끄고** `npm run check`
- 화면을 바꿨으면 **실제로 띄워서 눈으로 본다**
- `web/CHANGELOG.md`에 **"왜"** 를 남긴다
- 배포 후 **운영본을 검증한다.** 로컬 통과가 배포 성공이 아니다
- 지시가 `docs/운영지침.md`와 어긋나면 **먼저 말하고**, 진행 여부는 사람이 정한다
