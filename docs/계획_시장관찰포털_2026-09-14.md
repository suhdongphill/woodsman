---
날짜: 2026-09-14
작성: Claude Opus 5 (Claude Code)
요청: 운영자 — 「다양한 요구사항이지만 목표는 같으니 잘 정리해서 작업」 · 「포탈은 시장을 계속 관찰하고 독자에게 독특한 정보를 전달」
상태: **v0.1 — 통합 작업 계획.** 한 조각 = 한 배포(운영지침 §3-1)
관련: `woodsman_개발요구서_v2.md` 운영자 원칙 ②~⑦ · `설계_홈_섹션계획.md` · `설계_자본레짐엔진.md` 10~12장
---

# 시장 관찰 포털 — 통합 작업 계획

## 0. 목표 한 줄

> **포털은 시장을 계속 관찰하고, 독자에게 독특한 정보를 전달한다.**
> 독특함은 세 가지에서 나온다 — ① **우리가 계산한 판정**(산식 공개 · LLM이 점수를 정하지 않음) ② **조류·바람·파도로 나눈 읽기**
> ③ **나무꾼이 그 속에서 실제로 어떻게 하는지**(계좌·일지 · 산장).

## 1. 이번에 받은 요구를 목표에 붙인다

| 요구(2026-09-14) | 목표의 어디 | 조각 |
|---|---|---|
| ChatGPT는 유동성 점수를 내는데 우리는 판정 보류 — 산출 방법을 찾아라 | ① 우리가 계산한 판정 | **S1** 전년비 버그 수정 → GLS 발행 · **S2** 입력 확장 |
| 유동성 판정이 되도록 항목을 가져오는 방법 | ① | **S2** |
| 바람 아래 금리·유가·지정학·연준 발언·CPI·FOMC 기사 프레임 | ② 파도 | **S3** |
| 유동성 카드 한 줄 요약 · 누르면 점수 · 해석 · 서버 저장 분석 팝업 | ①② | **S4** |
| 조류가 세면 나무꾼은 산장에서 지켜본다 | ③ | **S5** |
| 모델 최신성 · 자동이 안 돌면 수동 | 「계속 관찰」의 전제 | ✅ 배포(`b6df3be`) |

## 2. 조각

### 진행 기록

| 조각 | 상태 |
|---|---|
| S1 전년비 | ✅ 배포 `cfbd680` — 운영 점수는 **다음 수집 뒤** 다시 계산된다(대시보드 「지금 수동 수집」) |
| S2-a 자금시장 | ✅ 배포 `823368a` — 레포 안정성(SOFR 99−1) · 자금 변동성 → 자금시장 65% · **GLS 50.1 · 80% 정상 발행**(읽기 전용 계산) |
| S2-b 재무부 | ⚠ 배포 `908eebc` — **운영에서 효과 없음**(9/13 15:19 UTC 수집: TreasuryDirect도 525). 워커 → treasury.gov TLS가 막히는 것으로 보임(미확정). 해석 코드는 유지 · **워커 밖에서 받아 오는 경로 필요**(S2-c 제안) |
| 운영 반영(9/13 15:19 UTC 수집) | ✅ GLS 50.1 · 80% 정상 발행 · 4주 전 41.6 · 파도 기사 54 · 선물·DFF·SOFR 분포 · ⚠ 실패 5(재무부 4 · FRED HOUST 403 두 번째) |
| S3a 파도(자동) | ✅ 배포 `dce84df` — 연준 RSS 셋 + BLS CPI → `MacroNews` → 홈 바람 아래 · 본문 미저장 · MANUAL 보호 |
| S3b 파도(관리자) | ✅ 배포 `f007f4c` — `/admin/news` 운영자 입력(원문 링크 https · 요약 200자 · 본문 칸 없음) · 숨기기 · 파도 해석 수정(연준 빈 날짜 1899 · 발언자는 링크 파일명 확인) |
| S4 유동성 팝업 | ✅(배포 전) 카드 한 줄 요약(프로그램) · 「숫자 뜯어보기 · 해석」 팝업 · `/admin/analysis` 붙여넣기(산식 없는 점수 · Confidence 점검 — 지우지 않고 줄 번호로 경고) |
| S2-d 읽는 창 | ✅(배포 전) ⚠ **계획에 없던 버그** — 화면이 계열당 14점만 읽어 주간 전년비·20일 실현변동성 **넷이 영구 「미수집」**이었다. 필요량을 카탈로그가 정하게 고침(`lib/macro/window.ts`). 점수 엔진은 영향 없었다(날짜 범위로 따로 읽는다) |
| 다음 | 운영 「지금 수동 수집」 → 점수 재계산·기사 수집 확인 · S3b 관리자 기사 입력(유가·지정학) · S2-c 재무부 순발행·바이백(대체 경로 필요) · S4 유동성 팝업 |

### ⭐ 다음 세션 인수인계 (2026-09-14 마무리)

**운영에 올라간 것(오늘)**: 점수 엔진 → 조류(유동성·금리 방향·버블·엔진 온도) → 모델 최신성 대시보드 → 자동 수집 하루 두 번(평일 22:00 KST 추가) →
전년비 버그 수정(GLS 발행) → 자금시장 보강 → 파도(연준·BLS 자동 + 관리자 입력) → 유동성 한 줄 요약·해석 팝업·그날의 분석. 마지막 배포 `e37883d`.

**운영 상태(9/13 15:19 UTC 수집 기준)**: GLS **50.1 · 80% · 정상 발행** · 4주 전 41.6(넉넉해지는 쪽) · 파도 기사 54건 · 점수 계산은 수집 뒤 자동.

**결정된 다음 일 — 순서대로**
1. **S2-c 재무부 수집을 GitHub Actions 예약 작업으로** (운영자 결정 2026-09-14)
   - 이유: 워커 → treasury.gov가 **525**(Fiscal Data · TreasuryDirect 둘 다). 로컬은 받아진다.
   - 설계: 평일 **20:30 UTC(05:30 KST)** — 06:00 자동 수집 **직전**에 돌아, 그 수집 뒤 점수 계산에 재무부 값이 들어간다(새 비밀값 불필요).
     기존 `CLOUDFLARE_API_TOKEN` · `CLOUDFLARE_ACCOUNT_ID` 사용 · 쓰기는 **쿼리 경로(`--command`) 한 줄씩**(`--file` 금지) ·
     **L1(`MacroObservation`) 먼저, L2(`MacroPoint`) 다음**(`diffObservations` 재사용) · 해석은 테스트된 `lib/macro/treasury.ts`
     (`mspdShares` · `nominalTenYearAuctions` · `treasuryDirectToAuctionRow`) · 재시도는 `scripts/alfred-backfill.mjs`의 `d1Query` 방식.
   - 결과를 `MacroIngest`에 `trigger = GITHUB_TREASURY`로 남긴다 → 최신성 대시보드에 줄 추가.
   - 워커 수집에서는 `TREASURY` 출처를 **뺀다**(매일 실패 4건 소음 제거) — 대시보드가 외부 작업의 성공·실패를 대신 보여 준다.
   - 효과 기대: 재무부 계기(현재 30%)에 입찰 응찰률(0.10)이 들어가도 40% → 순발행 압력·바이백까지 붙여야 60% 발행선.
2. ~~**FRED `HOUST`(주택착공) 403**~~ — ⚠ **2026-09-16 정정: 재현되지 않는다.** HOUST는 정상 수집됐고(1,239천호), 9/14에는 `semi_ip_yoy`가, 9/15 두 번에는 아무 계열도 403이 아니었다. **계열 고유 문제가 아니라 FRED가 매번 다른 계열 하나에 내는 간헐적 403**이다 — 우선순위를 내린다. 수집 성공 92 중 1건 이하로 계속 뜨면 그때 본다.
3. **그날의 분석 첫 게시** — 운영자가 9/13 보고서를 `/admin/analysis`에 붙여넣기(산식 없는 점수·Confidence 줄은 점검이 줄 번호로 알려 준다).
4. **S5 산장** — `설계_홈_섹션계획.md` §3 조건 초안(국채 실현변동성 67↑ + GLS 40↓/빡빡 + 스트레스 + 지속성) · 백테스트로 문턱 교정 후 운영자 확인.
5. 남은 운영자 결정: 버블 「미 반도체 8사 P/S」 재계산(8사 고정 뒤 1차 출처로) · 준칙 모형 상수(r* 0.5 · u* 4.2) 최신화 여부(볼트와 함께) ·
   서머타임 해제(11월) 뒤 22:00 수집이 발표(22:30 KST)보다 이른 문제.

**주의(오늘 배운 것)**
- 로컬에서 되는 외부 호출이 워커에서는 막힐 수 있다 — 새 출처는 **운영 수집 결과로** 확인한 뒤에야 「해결」이라 적는다(S2-b 정정).
- 한 줄 `node -e`의 셸 인용이 정규식을 깨뜨린다 — 확인 스크립트는 파일로.
- 연달아 push하면 진행 중 배포가 취소된다 — 배포 확인 뒤 다음 push.

### S1 — 주간 전년비 버그 수정 → GLS 발행

- 원인: 전년비 변환이 「정확히 1년 전 같은 날짜」만 찾아 **주간(수요일) 계열의 짝이 없었다** → 은행 신용 전년비 결측 → 신용 유동성 50% → GLS 50%(보류).
- 수정: 1년 전 **±3일** 안의 가장 가까운 점. 월간은 그대로 정확히 맞는다.
- ⭐ 운영 데이터로 확인(읽기 전용): 신용 65%(61.5) → **GLS 50.4 · 커버리지 65% · 🟡 낮은 신뢰로 발행**. 같은 날 ChatGPT 모니터 52.
- 배포 뒤 첫 계산: 다음 수집(대시보드 「지금 수동 수집」 또는 06:00 자동).

### S2 — 유동성 입력 확장: 판정을 단단하게 (커버리지 65% → 목표 90%+)

| 하위 점수 | 지금 | 붙일 것 | 출처(무료) | 효과 |
|---|---|---|---|---|
| 자금시장(§15) 35% | SOFR−IORB | **레포 안정성** = SOFR 99백분위 − 1백분위 · **자금 변동성** = SOFR 일간 변화 20일 실현변동성 | FRED `SOFR99`·`SOFR1`(2018-04~) · 기존 `sofr` + `realizedVolBp` | 35 → **65%** 발행 |
| 재무부(§14) 30% | TGA 변화 | ⚠ **입찰 응찰률이 운영에 0행 — 원인 확인됨(9/13 22:43 수집)**: 워커 → fiscaldata.treasury.gov가 **525(TLS 핸드셰이크 실패)·시간 초과**로 4계열 전부 실패. 로컬은 성공. **우회 경로부터**(S2 첫 일) · 순발행 압력 = 시장성 국채 총액 3개월 변화 · 바이백 | Fiscal Data MSPD 합계 · Buybacks (⚠ 워커에서 막힘) | 30 → **65%** 발행 |
| SRF 이용 | — | ⚠ `RPONTSYD`는 거의 매일 0 → 10년 창 흩어짐(MAD)=0이라 **정규화 불가** — 결측 이유로 남긴다 | FRED | — |
| 결과 | GLS 65% | 재무부·자금시장이 발행되면 **GLS 100% 구성** | | |

- ⚠ 새 측정은 명세가 정하지 않은 자리(레포 안정성의 정의)라 **Woodsman v0 가정**으로 설계서 12-2에 적는다.
- ⚠ H.4.1 TGA는 **주간 평균**, 우리 `tga`(WDTGAL)는 **수요일 잔액**이다 — 둘 다 맞고 정의가 다르다. 카드에 명시한다(ChatGPT 9/13 보고서 $883.3B vs 우리 $843.7B).

### S3 — 파도: 오늘의 기사 (바람 바로 아래 · 운영자 결정)

- 자리: `macroStrip`(바람) → **`waves`(파도)** → `tide`(조류). 가로 한 줄 5~6건, 낮게.
- 자동: **연준 공식 RSS**(의장·이사 연설 · FOMC 성명 · 의회 증언) · **BLS CPI 발표(Atom)** — 공공 저작물. 수집 때 함께 받는다.
- 수동: 관리자 입력 — 유가·지정학·환율·정책 충격(링크 · 날짜 · 출처 · 한 줄 요약 · 분류).
- ⚠ 저작권: **본문을 싣지 않는다** — 제목 · 날짜 · 출처 링크 · 우리 요약만. 분류: 금리 · 물가(CPI) · 연준(FOMC·발언) · 유가 · 지정학 · 환율.
- ⚠ 원칙 ③: 기사를 점수로 바꾸지 않는다. 사건은 날짜와 출처로 지표와 같은 시간축에 잇는다.

### S4 — 유동성 카드: 한 줄 요약 · 점수 상세 · 해석 팝업 (운영자 결정 2026-09-14)

- 카드 아래 **한 줄 요약** — 프로그램이 쓴다(점수 · 방향 · 가장 크게 끌어올린/내린 계기). 예: 「유동성 50 · 보합 — 신용(62)이 받치고 금리시장(40)이 누른다」.
- 카드를 누르면 **점수 상세**(하위 계기 · 기여도 · 결측 이유 · 기준일) → 「해석」을 누르면 **팝업**.
- 팝업 = ① 우리 계산의 해석(프로그램 문장) ② **그날의 분석** — 관리자가 붙여넣은 일일 보고서(저장: 서버 · 마크다운 → 정화 HTML, 기존 글 경로).
- ⚠ **운영자 결정**: 점수는 **우리 계산**으로 낸다. 외부 보고서(ChatGPT 모니터)에서는 **출처 있는 사실과 해석 문장만** 싣고, **산식 없는 점수·Confidence %는 뺀다**(개발요구서 v2 원칙 유지).
- ⚠ 「한 줄 결론」은 관리자가 따로 적는다 — 보고서 본문에서 자동으로 뽑지 않는다.

#### S4 설계 (2026-09-14 초안)

**1) 카드 아래 한 줄 요약 — 프로그램이 쓴다(LLM 없음)**
- 입력: 오늘 LIVE `ScoreValue`(GLS) 한 행의 `detail` — 구성요소 점수 · 가중치 · 결측 이유 + 4주 전 행.
- 규칙(순수 `lib/scores/summary.ts` · 테스트):
  1. 「유동성 {점수} · {방향 말}」 — 방향은 `tideDirection`(4주).
  2. 발행된 구성요소 중 **50에서 가장 위**인 것 = 「받치는 계기」, **가장 아래**인 것 = 「누르는 계기」(차이가 5점 미만이면 말하지 않는다).
  3. 커버리지 80% 미만이면 「(빠진 계기: …)」를 붙인다.
- 예: 「유동성 50 · 보합 — 신용(62)이 받치고 금리시장(40)이 누른다 · 빠진 계기: 재무부」.
- ⚠ 판단 문장이 아니라 **구성의 서술**이다 — 「사라/팔라」가 들어갈 자리가 없다(테스트가 권유 단어를 막는다).

**2) 누르면 점수 상세 → 「해석」 팝업**
- 카드 전체를 버튼으로(키보드 가능) → `<dialog>` 팝업. ⚠ 클라이언트 JS는 이 팝업 하나만(서버 컴포넌트가 내용을 미리 그려 넘긴다 — 열 때 요청 없음).
- 팝업 1부 **우리 계산**: 하위 계기 표(점수 · 가중치 · 기여 점수 · 결측 이유 · 기준일) · 13주/4주 방향 · 산식 링크(설계서 11~12장 공개 페이지는 R5).
- 팝업 2부 **그날의 분석**: 관리자가 붙여넣은 일일 보고서.

**3) 그날의 분석 — 저장**
- 테이블 `DailyAnalysis`(date PK · oneLine · body 마크다운 · bodyHtml(정화) · sourceLabel · updatedAt). ⚠ 글 본문 저장 경로(`body` → `lib/markdown` → `lib/sanitize-html` → `bodyHtml`)를 **그대로** 쓴다(CLAUDE.md §6).
- 관리자 화면 `/admin/analysis`: 날짜 · 한 줄 결론(필수 · 120자) · 본문 붙여넣기 · 출처 표기(기본 「운영자 정리 · 외부 AI 분석 참고」).
- ⚠ **운영자 결정(2026-09-14)**: 외부 보고서의 **산식 없는 점수·Confidence %는 싣지 않는다**. 저장 전에 경고한다 —
  `\b\d{1,3}\s*/\s*100\b` · `Confidence` · `\d{1,3}%`(경쟁가설 표 모양)를 찾아 「지우고 저장하시겠습니까」를 띄운다(자동으로 지우지는 않는다 — 운영자가 문장을 고른다).
- 홈 카드는 **오늘 또는 가장 최근 분석의 한 줄 결론**을 팝업 머리에 날짜와 함께 둔다. ⚠ 사흘보다 오래됐으면 「{날짜} 분석」이라고 날짜를 크게.

### S6 — 버블 시그널: 볼트의 근거를 포털로 (2026-09-16 · 나중에)

운영자 요청으로 `D:\Woodsman\Investor/_apps/메모리 버블 트리거.html`을 대조했다. **지표 키는 포털 `/admin/bubble`과 1:1로 같다**
(`capex_yoy` · `semi_ps` · `hy_oas` …). 오히려 포털이 앞선 데도 있다 — 볼트는 아직 `MOVE`라 부르고 포털은 「국채 변동성(실현, MOVE 대용)」으로 정정돼 있다.

**포털에 자리가 없는 것은 `signals` 35건이다.** 각 항목이 이름 · 해설 · `dir`(방향) · `tone`(bull/bear/neutral) · `read`(한 줄 읽기) ·
`src`(출처) · `date`를 갖는다. 9/15에 넷이 늘었다(D램 현물가의 대표성 · 블렌디드 ASP 크로스오버 · AI 속도 조절론 · 데이터센터 수요처 다변화).

- 왜 맞나: **출처와 날짜가 달린 해석 · bull/bear 양쪽 병기 · 점수가 아닌 서술.** 「파도」가 기사 제목 줄이라면 이건 그 아래 층이다(원칙 ③과 어긋나지 않는다).
- 초안: `BubbleSignal`(id · name · body · dir · tone · read · src · date · hidden) + `/admin/bubble` 시그널 탭. 공개 노출은 **다음 조각**으로 가른다.
- ⚠ 저작권 선은 파도 기사와 같다 — 본문을 옮기지 않고 **우리 요약과 원문 링크**만.
- ⚠ **`chains`(KR 가치사슬 액션)는 옮기지 않는다** — 「비중 확대 가능」·「후기테마부터 축소」는 종목별 매매 권고라 개발요구서 v2와 충돌한다.
  옮긴다면 **레짐 서술까지만**이다.
- ⚠ 포털에 공개 `/bubble` 페이지가 **아직 없다**(관리자 전용). S6를 공개까지 끌고 가려면 그 조각이 먼저다.

### S5 — 산장 (원칙 ⑦) — 조건 초안 확인 뒤

- `설계_홈_섹션계획.md` §3. 국채 실현변동성 점수 · GLS 방향 · 스트레스 · 지속성. 백테스트로 문턱 교정.

## 3. 순서의 이유

1. **S1이 먼저** — 코드 한 줄짜리 버그가 앞줄 판정 전체를 막고 있었다. 가장 싸고 효과가 크다.
2. **S2가 S4보다 먼저** — 팝업이 보여 줄 「점수 상세」가 결측투성이면 해석이 결측을 설명하는 글이 된다.
3. **S3은 S2와 독립** — 기사 파이프라인은 점수와 섞이지 않는다. S2가 길어지면 S3을 먼저 배포해도 된다.
4. **S5는 문턱 확인 뒤** — 조건이 틀리면 나무꾼의 태도가 틀린 신호를 낸다.

## 개정

| 판 | 날짜 | 무엇 |
|---|---|---|
| v0.1 | 2026-09-14 | 요구 여섯 가지를 목표 하나에 붙임 · S1~S5 · 운영자 결정(분석 게시 방식 · 입력 방식 · 기사 자리 · 기사 공급) |
