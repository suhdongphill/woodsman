# 🛠️ StockLens — Claude Code 단계별 프롬프트

> **사용법**: 각 단계를 순서대로 Claude Code에 붙여넣으세요.  
> 이전 단계가 완료된 것을 확인한 후 다음 단계로 넘어가세요.

---

## STEP 1 — 프로젝트 뼈대 + 포트폴리오 CRUD + localStorage

```
StockLens라는 개인 주식 포트폴리오 관리 웹앱을 만들어줘.
단일 HTML 파일(index.html)로 만들고, CDN으로 React 18, Tailwind CSS, Chart.js를 로드해.
서버 없이 브라우저에서 바로 열리는 구조야.

──────────────────────────────────────────
[데이터 모델] localStorage key: "stocklens_portfolio"
──────────────────────────────────────────
StockHolding {
  id: string            // crypto.randomUUID()
  ticker: string        // "AAPL" or "005930.KS"
  name: string          // 종목명
  industry: string      // 산업 카테고리
  avgCost: number       // 평균매입단가
  shares: number        // 보유 수량
  currency: "KRW"|"USD"
  weight: "확대"|"유지"|"축소"
  investIdea: string    // 투자 아이디어 메모
  addedAt: string       // ISO string
  updatedAt: string
}

──────────────────────────────────────────
[구현할 기능]
──────────────────────────────────────────
1. 앱 초기화
   - localStorage에서 포트폴리오 데이터 로드
   - 데이터 없으면 빈 배열로 시작

2. 대시보드 (메인 화면)
   - 상단 헤더: 앱 이름 "StockLens" + "종목 추가" 버튼
   - 보유 종목 카드 그리드 (종목명, 티커, 산업, 평균매입단가, 수량, 비중전략 배지 표시)
   - 종목이 없으면 "포트폴리오에 종목을 추가하세요" 빈 상태 화면

3. 종목 추가 모달
   - 입력 필드: 티커(필수), 종목명(필수), 평균매입단가(필수), 수량(필수)
   - 드롭다운: 통화(KRW/USD), 산업 카테고리, 비중전략(확대/유지/축소)
   - 텍스트에어리어: 투자 아이디어
   - 산업 카테고리 옵션: 반도체·IT부품 / 소프트웨어·플랫폼 / 바이오·헬스케어 /
     소비재·유통 / 금융·핀테크 / 에너지·소재 / 산업재·기계 / 통신·미디어 / 기타
   - 저장 시 localStorage 업데이트 후 모달 닫기

4. 종목 수정
   - 각 카드에 "수정" 버튼 → 기존 값이 채워진 수정 모달 열기
   - 저장 시 updatedAt 갱신

5. 종목 삭제
   - 각 카드에 "삭제" 버튼 → "정말 삭제하시겠습니까?" 확인 모달
   - 확인 시 localStorage에서 제거

──────────────────────────────────────────
[UI 디자인 방향]
──────────────────────────────────────────
- 다크 테마 (배경 #0f1117, 카드 #1a1d27)
- 포인트 컬러: 에메랄드 그린 (#10b981)
- 수익 양수: 초록, 음수: 빨강
- 비중 배지: 확대=초록, 유지=노랑, 축소=빨강
- 폰트: Google Fonts에서 "Pretendard" 또는 "Noto Sans KR"
- 카드에 hover 시 미세한 상승 애니메이션
- 모달은 backdrop blur 처리

──────────────────────────────────────────
[기술 제약]
──────────────────────────────────────────
- 외부 API 호출 없음 (이 단계에서는 더미 데이터 사용)
- 현재가 필드는 일단 avgCost로 대체 (다음 단계에서 연동)
- 모든 상태는 React useState로 관리
- 페이지 새로고침 후에도 데이터 유지 확인
```

---

## STEP 2 — Yahoo Finance 연동 (현재가 + 90일 차트)

```
기존 index.html에 Yahoo Finance API 연동을 추가해줘.
CORS 우회를 위해 모든 Yahoo Finance 요청 앞에 "https://corsproxy.io/?" 프록시를 붙여.

──────────────────────────────────────────
[추가할 기능]
──────────────────────────────────────────
1. 현재가 자동 조회
   - 앱 로드 시 + "새로고침" 버튼 클릭 시 전체 보유 종목 현재가 일괄 조회
   - API: GET https://corsproxy.io/?https://query1.finance.yahoo.com/v8/finance/chart/{ticker}
   - 응답에서 meta.regularMarketPrice 추출
   - 각 카드에 현재가, 수익률(%), 평가손익 표시
   - 수익률 = (현재가 - 평균매입단가) / 평균매입단가 * 100
   - 조회 실패 시 카드에 "가격 조회 실패" 표시 + 재시도 아이콘

2. 종목 상세 뷰 (카드 클릭 시 슬라이드인 패널 또는 모달)
   헤더 영역:
   - 종목명, 티커, 산업 배지, 거래소
   - 현재가 / 평균매입단가 / 수익률 / 평가손익 (크게 표시)

   90일 주가 차트 (Chart.js Line):
   - API: GET https://corsproxy.io/?https://query1.finance.yahoo.com/v8/finance/chart/{ticker}?range=3mo&interval=1d
   - 응답 timestamps + indicators.quote[0].close 사용
   - X축: 날짜, Y축: 종가
   - 평균매입단가를 수평 기준선(dashed)으로 표시
   - 라인 색상: 현재가 > 매입단가이면 에메랄드, 아니면 빨강

   90일 거래량 차트 (Chart.js Bar):
   - 같은 API 응답의 indicators.quote[0].volume 사용
   - 거래량 단위: 백만주(M) 또는 천주(K) 자동 변환
   - 바 색상: 전일 대비 상승이면 초록, 하락이면 빨강

3. 로딩 상태 처리
   - 차트 로딩 중: 스켈레톤 애니메이션 또는 스피너
   - API 실패: 에러 메시지 + "다시 시도" 버튼

──────────────────────────────────────────
[캐싱 전략]
──────────────────────────────────────────
- 현재가는 매 조회마다 새로 가져옴 (캐시 없음)
- 차트 데이터는 sessionStorage에 캐시 (key: "chart_{ticker}")
  → 같은 세션 내 재방문 시 API 재호출 생략
- 차트 캐시 만료: 30분 (timestamp 비교)

──────────────────────────────────────────
[기존 기능 유지]
──────────────────────────────────────────
- CRUD, localStorage 저장 기능 그대로 유지
- 대시보드 카드에 현재가·수익률 컬럼 추가
```

---

## STEP 3 — Claude AI 연동 (CANSLIM 점수 + 최신 동향)

```
index.html에 Anthropic Claude API를 이용한 CANSLIM 점수 분석과 최신 동향 기능을 추가해줘.

──────────────────────────────────────────
[API 설정]
──────────────────────────────────────────
- Endpoint: https://api.anthropic.com/v1/messages
- Model: claude-sonnet-4-20250514
- 헤더: "x-api-key"는 사용자가 앱 내 설정 화면에서 직접 입력하도록 처리
  (API 키를 하드코딩하지 말 것)
- API 키는 localStorage key "stocklens_api_key"에 저장

──────────────────────────────────────────
[기능 1] CANSLIM 자동 점수 평가
──────────────────────────────────────────
종목 상세 뷰에 "CANSLIM 분석" 버튼 추가.
버튼 클릭 시 다음 프롬프트로 Claude API 호출:

  system: "당신은 CANSLIM 투자 방법론 전문가입니다. 주어진 종목을 분석하여
           반드시 아래 JSON 형식으로만 응답하세요. 다른 텍스트는 포함하지 마세요."

  user: "종목명: {name}, 티커: {ticker}
         이 종목을 CANSLIM 방법론으로 분석하여 각 항목을 0~10점으로 평가하세요.
         {
           'C': {'score': 숫자, 'reason': '한 줄 근거'},
           'A': {'score': 숫자, 'reason': '한 줄 근거'},
           'N': {'score': 숫자, 'reason': '한 줄 근거'},
           'S': {'score': 숫자, 'reason': '한 줄 근거'},
           'L': {'score': 숫자, 'reason': '한 줄 근거'},
           'I': {'score': 숫자, 'reason': '한 줄 근거'},
           'M': {'score': 숫자, 'reason': '한 줄 근거'},
           'composite': 숫자 (7개 항목 평균, 소수점 1자리)
         }"

결과 표시:
- 7개 항목 레이더 차트 (Chart.js Radar)
- 항목별 점수 바 + 근거 텍스트
- 종합 점수 (10점 만점, 크게 표시)
- 점수 기준선: 7점↑ 초록, 5~7 노랑, 5↓ 빨강
- 분석 결과는 localStorage에 캐시 (key: "canslim_{ticker}", 24시간 유효)

──────────────────────────────────────────
[기능 2] 최신 동향 요약
──────────────────────────────────────────
종목 상세 뷰에 "최신 동향 분석" 버튼 추가.
버튼 클릭 시 Claude API 호출:

  system: "당신은 주식 시장 분석가입니다. 간결하고 핵심적인 정보만 제공하세요."

  user: "종목명: {name} (티커: {ticker})에 대해 다음을 분석해주세요:
         1. 최근 주요 이벤트 및 뉴스 (3~5개 불릿)
         2. 현재 시장에서의 포지션 (2~3문장)
         3. 단기 주목 포인트 (2~3문장)
         한국어로 작성하고, 각 섹션을 명확히 구분해주세요."

결과 표시:
- 마크다운 렌더링 (간단한 파서로 불릿·볼드 처리)
- 분석 결과 하단에 "분석 시각: {timestamp}" 표시
- 결과는 sessionStorage에 캐시 (1시간)

──────────────────────────────────────────
[API 키 설정 화면]
──────────────────────────────────────────
- 우측 상단 ⚙️ 아이콘 클릭 시 설정 모달 오픈
- API 키 입력 필드 (password 타입, 눈 아이콘으로 토글)
- "저장" 시 localStorage에 보관
- API 키 미설정 시 CANSLIM/동향 버튼 클릭하면
  "API 키를 먼저 설정해주세요" 안내 + 설정 모달 자동 오픈

──────────────────────────────────────────
[로딩 & 에러 처리]
──────────────────────────────────────────
- AI 분석 중: "분석 중..." 텍스트 + 펄싱 애니메이션
- API 오류: 에러 메시지 표시 + "다시 시도" 버튼
- JSON 파싱 실패 시 재시도 1회 자동 실행
```

---

## STEP 4 — 포트폴리오 대시보드 + 산업 분류 차트

```
index.html 대시보드를 강화해줘.
포트폴리오 전체 요약 패널과 산업별 비중 차트를 추가해.

──────────────────────────────────────────
[요약 지표 패널 (대시보드 상단)]
──────────────────────────────────────────
다음 4개 지표 카드를 가로로 배치:
1. 총 투자원금
   = Σ (avgCost × shares), KRW/USD 구분 합산
2. 현재 평가금액
   = Σ (현재가 × shares)
3. 전체 수익률(%)
   = (평가금액 - 원금) / 원금 × 100
   → 양수면 초록 ▲, 음수면 빨강 ▼
4. 포트폴리오 CANSLIM 평균
   = localStorage에 저장된 각 종목 composite 평균
   → 점수가 없는 종목은 제외하고 계산, "(n개 종목 기준)" 표시

──────────────────────────────────────────
[산업별 비중 차트]
──────────────────────────────────────────
- Chart.js Doughnut 차트
- 각 섹터: 업종별 평가금액 합산 / 전체 평가금액
- 차트 중앙: 총 종목 수 표시
- 범례: 오른쪽에 업종명 + 비중% 표시
- 색상 팔레트: 에메랄드, 블루, 퍼플, 오렌지, 핑크, 옐로우 등 구분

──────────────────────────────────────────
[종목 카드 개선]
──────────────────────────────────────────
기존 카드에 다음 정보 추가:
- 포트폴리오 내 비중% (해당 종목 평가금액 / 전체)
- CANSLIM 종합 점수 (저장된 경우만 배지로 표시)
- 수익률 색상 강조

카드 정렬 옵션 (드롭다운):
- 추가일 최신순 (기본)
- 수익률 높은순
- 평가금액 높은순
- CANSLIM 점수 높은순

──────────────────────────────────────────
[필터 기능]
──────────────────────────────────────────
- 산업 카테고리별 필터 버튼 (전체 + 각 업종)
- 비중 전략별 필터 (전체 / 확대 / 유지 / 축소)
- 검색창: 종목명 또는 티커 실시간 검색

──────────────────────────────────────────
[기존 기능 유지]
──────────────────────────────────────────
- CRUD, 현재가 조회, 상세 뷰, AI 분석 모두 그대로 유지
- 요약 패널은 현재가 갱신 시 자동 업데이트
```

---

## STEP 5 — 데이터 관리 + 최종 폴리싱

```
index.html 마지막 완성 단계야.
데이터 내보내기/가져오기 기능과 전체 UI를 다듬어줘.

──────────────────────────────────────────
[데이터 관리 기능]
──────────────────────────────────────────
1. JSON 내보내기
   - 우측 상단 메뉴에 "내보내기" 버튼
   - localStorage 전체 포트폴리오 데이터를 JSON 파일로 다운로드
   - 파일명: "stocklens_backup_{YYYYMMDD}.json"
   - 마지막 내보내기 날짜를 localStorage에 기록

2. JSON 가져오기
   - "가져오기" 버튼 → 파일 선택 다이얼로그
   - 파일 유효성 검사 (StockHolding 구조 확인)
   - "기존 데이터에 병합" vs "전체 교체" 선택 모달
   - 성공 시 토스트 알림 표시

3. 백업 알림
   - 마지막 내보내기로부터 30일 경과 시 상단 배너 표시
   - "지금 백업" 버튼으로 즉시 내보내기 실행

──────────────────────────────────────────
[UX 개선 사항]
──────────────────────────────────────────
1. 토스트 알림 시스템
   - 종목 추가/수정/삭제 성공: 초록 토스트
   - API 오류: 빨간 토스트
   - 우측 하단 표시, 3초 후 자동 사라짐

2. 키보드 단축키
   - N: 종목 추가 모달 열기
   - ESC: 모달 닫기
   - ?: 단축키 도움말 표시

3. 빈 상태(Empty State) 개선
   - 포트폴리오가 비어있을 때: 일러스트 + "첫 종목을 추가해보세요" 안내
   - 필터 결과가 없을 때: "검색 결과가 없습니다" 메시지

4. 반응형 레이아웃
   - 1280px 이상: 카드 4열 그리드
   - 768~1280px: 2열 그리드
   - 768px 미만: 1열 리스트

5. 로딩 최적화
   - 앱 초기 로딩 시 현재가 자동 조회 (백그라운드)
   - 각 카드 로딩 상태 스켈레톤 개별 표시

──────────────────────────────────────────
[최종 검수 체크리스트]
──────────────────────────────────────────
아래 항목을 모두 확인하고 완성해:

[ ] 새로고침 후 데이터 유지 확인
[ ] 모든 모달 ESC 키로 닫기
[ ] 차트가 없는 종목(API 실패)에서 에러 없이 작동
[ ] API 키 미설정 시 AI 기능 graceful 처리
[ ] KRW/USD 혼재 포트폴리오에서 수익률 계산 오류 없음
[ ] 종목 0개일 때 대시보드 요약 패널 0 표시 (NaN 없음)
[ ] 모바일 768px에서 레이아웃 깨짐 없음
[ ] 다크모드 전체 일관성

──────────────────────────────────────────
[최종 파일 구조]
──────────────────────────────────────────
단일 파일 index.html 하나로 완성.
CDN 의존성:
  - React 18 (unpkg)
  - ReactDOM 18 (unpkg)
  - Babel Standalone (JSX 처리)
  - Chart.js 4.x (cdnjs)
  - Tailwind CSS (cdn.tailwindcss.com)
  - Google Fonts (Noto Sans KR)
```

---

## 📌 단계별 실행 팁

| 단계 | 예상 소요 시간 | 완료 기준 |
|------|--------------|-----------|
| STEP 1 | 20~30분 | 종목 추가/수정/삭제 + 새로고침 데이터 유지 |
| STEP 2 | 20~30분 | 종목 클릭 시 차트 2개 정상 렌더링 |
| STEP 3 | 15~20분 | API 키 입력 후 CANSLIM 점수 JSON 파싱 성공 |
| STEP 4 | 15~20분 | 요약 패널 수치 + 도넛 차트 정상 출력 |
| STEP 5 | 15~20분 | JSON 내보내기 파일 다운로드 + 토스트 작동 |

> **주의**: STEP 3 실행 전 반드시 Anthropic API 키를 준비하세요.  
> STEP 2의 Yahoo Finance CORS 오류 발생 시 `corsproxy.io` 대신  
> `https://api.allorigins.win/raw?url=` 으로 교체해 보세요.
