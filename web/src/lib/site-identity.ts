/**
 * 사이트가 **자기를 뭐라고 소개하는가** — 한 곳에서만 정한다.
 *
 * ## 왜 뺐나 (2026-08-31, 홈 재편 Step 5)
 * 같은 문장이 세 군데에 따로 박혀 있었다 — `app/layout.tsx`의 메타, `lib/llms-txt.ts`의
 * 첫 줄, 그리고 홈에 새로 붙일 JSON-LD. **검색·AI가 읽는 문장**이 흩어져 있으면
 * 하나만 고치고 나머지가 뒤처진다.
 *
 * ⚠ 이 사고는 이미 두 번 났다. 2026-08-25에 화면을 고치고 메타를 두었고,
 *    2026-08-30에 홈이 흐름 이야기로 바뀌었는데 **검색결과는 계좌 이야기**였다.
 *    화면은 눈에 보이니까 고쳐지고, 이 문장들은 안 보이니까 안 고쳐진다.
 *
 * ⚠ **숫자를 여기 적지 않는다.** 정적 문자열이라 지표가 늘면 조용히 거짓이 된다.
 *    수를 세어 말하는 자리는 `lib/llms-txt.ts`뿐이고, 거기서는 실제로 센다.
 * ⚠ **필명으로 적는다.** 공개 화면의 주체는 Woodsman이다(실명은 `/privacy` 한 곳뿐).
 */

/** 발행 주체. 공개 화면의 이름이다. */
export const SITE_NAME = "Woodsman";

/** 한 줄 성격 — 검색 제목의 뒷부분. */
export const SITE_TAGLINE = "거시 지표로 읽는 경제 흐름과 투자 기록";

/** 검색 결과의 제목. */
export const SITE_TITLE = `${SITE_NAME} — ${SITE_TAGLINE}`;

/**
 * 검색 결과의 본문(메타 설명)·JSON-LD의 description.
 * ⚠ **콘텐츠 사이트로 읽혀야 한다.** 남의 계좌 평가액을 검색하는 사람은 없고,
 *    「장단기 금리차」·「미시간 소비심리」를 검색하는 사람은 있다.
 */
export const SITE_DESCRIPTION =
  "금리·물가·유동성·고용 같은 거시 지표를 기준일·출처와 함께 공개하고, 침체 신호를 종합해 지금 경제가 어느 쪽으로 부는지 읽습니다. 그 흐름에 맞춘 포트폴리오와 판단 근거까지 그대로 남깁니다. 종목 추천이 아닌 정보 제공 목적입니다.";

/**
 * 공유 카드(OG)의 본문. 메타 설명보다 짧다 —
 * 카드에서는 두세 줄이 넘으면 잘리므로 **첫 문장이 관점을 말해야** 한다.
 */
export const SITE_OG_DESCRIPTION =
  "파도가 아니라 바람과 조류를 봅니다. 금리·물가·유동성이 어느 쪽으로 부는지 먼저 읽고, 그 흐름에 맞춘 판단을 그대로 남깁니다.";
