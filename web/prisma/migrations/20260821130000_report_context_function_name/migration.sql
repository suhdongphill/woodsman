-- 보고서 스냅숏에 **발행 시점의 분류 이름**을 함께 얼린다 (2026-08-21 결정).
--
-- 예전에는 분류 키만 얼렸다. 그러면 관리자가 "성장"을 "공격"으로 바꾸는 순간
-- ⚠ **이미 발행된 보고서의 문장까지 같이 바뀐다.** 보고서는 기록이다 —
--    발행 당시 "성장 버킷이라 산다"고 썼으면 그때의 판단이 그대로 남아야 한다.
--
-- ⚠ NULL을 허용한다. 이 결정 이전에 찍힌 스냅숏에는 이름이 없고,
--    그럴 땐 `reportFunctionLabel(key)`로 되돌린다. 빈 칸을 지어내지 않는다.
ALTER TABLE "StockReportContext" ADD COLUMN "functionName" TEXT;
